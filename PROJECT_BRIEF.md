# 東京散歩人 プロジェクト概要(セッションが変わっても最初に読むファイル)
最終更新: 2026-09-06。方針・体制に変更があった場合は、このファイルも都度更新すること。
## このファイルの目的
Claude(Cowork)は会話が長くなると内容が要約(圧縮)されることがあり、要約の粒度によっては細かい経緯が
失われる。このファイルは、次にセッションが変わった際に最初に読めば体制・確立した方法・現在地・
ハマりどころがすぐ分かるようにしておくためのもの。姉妹プロジェクトのwords-for-soul.com側にも
同種の`PROJECT_BRIEF.md`があるので、あわせて参照すること。
## このプロジェクトについて
「東京散歩人」は、Words for Soul(響)とは正反対の設計思想を取るブログ。響は「行っていない場所を
行ったと書かない」誠実さが価値の核だが、東京散歩人は「実際に歩く・食べる・飲む」という一次取材
そのものが価値の核。記事本文は、Ando様ご自身が実際に現地を歩いて取材してこない限り執筆できない
(Claude側だけでは着手できない)。詳細な文体ガイドは`persona_style_guide.md`を参照。
## 体制・役割分担
- Claude(Cowork): 企画・構成・本文執筆、Browser Claudeへの指示文の作成、進捗ログ内容の起草、
  GitHubのraw取得による独立検証を担当。ログイン情報を要する操作は直接実行できないため、指示文を
  作成してAndo様に中継してもらう。
- ChatGPT: 史跡・店舗情報の裏取り(ファクトチェック)を担当。
- 本人(Ando様): 一次取材(実際に歩く・食べる・飲む)、最終承認、Browser Claudeとの中継。
- Browser Claude: Ando様が既にログイン済みの実ブラウザ上で動く、別セッションのClaude。WordPress
  管理画面やConoHa WING管理画面での実装など、認証を伴う操作を実行する。Cowork側の会話内容を一切
  記憶していないため、依頼する指示文は毎回自己完結させる必要がある。
## サイトの基本情報(2026-09-06時点)
- ドメイン: sanpojin.tokyo(登録日2026-09-05)。**words-for-soul.com用のConoHa WING契約
  (WINGパック)に含まれる「2つ目の無料ドメイン」として取得されたもの**で、追加費用は発生していない
  (WINGパックには対象TLD限定で2つ目の独自ドメインが無料になる仕組みがあり、`.tokyo`が対象TLDに
  含まれる)。決済を伴わないため、取得した記憶が薄れやすい。取得自体はAndo様ご本人によるもので、
  不正な第三者によるものではないことを確認済み。
- ホスティング: ConoHa WING、サーバー契約名`wing-26-08-15-16-42`。この1契約の中に「サイト」が
  3つ登録されている: words-for-soul.com、sanpojin.tokyo、自動生成のデフォルトドメイン
  (`202608151642836ewy6b.conohawing.com`)。
- WordPressアドレス/サイトアドレスは`http://www.sanpojin.tokyo`(**wwwあり**)に設定されている。
  詳しくは下記の「ハマりどころ」を参照。
- サイトタイトル: 東京散歩人。キャッチフレーズ: 「歩いて、味わって、東京を知る。」
- テーマ: Cocoon(words-for-soul.comと同じ)。
- 管理者アカウント: user_loginは`jandokkr@gmail.com`(words-for-soul.comの管理者と同じメール
  アドレスだが、**パスワードは別のものを設定済み**。使い回していない)。
- 記事: まだ「Hello world!」のデフォルト投稿のみで、実記事は未執筆・未公開。第1回取材候補ルート
  (東京駅→日本橋→人形町)は候補リストとして作成済み(`walk01_tokyo-nihonbashi-ningyocho_candidates.md`)。
## ハマりどころ・確立した対処法(2026-09-06時点)
- **wwwなしでアクセスするとログインセッションが維持されない**。WordPressアドレス/サイトアドレスが
  `www.sanpojin.tokyo`に設定されているため、`www`なしのURL(`sanpojin.tokyo`)で管理画面に
  アクセスすると、ログインしてもすぐログアウト扱いになる。管理画面での作業は必ず`www`付きの
  URL(`https://www.sanpojin.tokyo/wp-admin/`)を使うこと。
- **WordPress管理者パスワードを紛失した場合、メールでの再設定が機能しないことがある**(原因は
  未特定、サーバー側のメール送信の問題と推測)。ConoHa WING管理画面には、このサイト単体の
  WordPress管理者パスワードを直接変更する機能は見当たらなかった。対処法として、以下の手順で
  データベースを直接編集する方法が有効だった。
  1. 「サイト管理」→対象サイト→「ファイルマネージャー」で`wp-config.php`を開き(閲覧のみ)、
     `DB_NAME`・`DB_USER`・`DB_HOST`の値を確認する。
  2. **注意**: このサーバー契約はマルチドメイン構成のため、対象サイト自身の「データベース」タブに
     データベースが表示されないことがある。その場合は、同じサーバー契約内の別のサイト
     (今回はwords-for-soul.com)の「データベース」タブを確認すると、そこに一覧が出てくる。
  3. 該当データベースのユーザー行を展開すると「管理ツール」欄があり、そこから直接phpMyAdmin
     (`https://phpmyadmin2010.conoha.ne.jp/`)にアクセスできる(このphpMyAdminへのログインには
     wp-config.phpのDB_USER・DB_PASSWORDが必要で、Ando様ご自身の入力が必要)。
  4. phpMyAdminで`wp_users`テーブルの該当行を編集する際、`user_pass`列の「関数」を**MD5**に
     設定し、値の欄に新しいパスワードを平文で入力して保存する。WordPress側は次回ログイン時に
     この形式を古い(レガシー)ハッシュ形式として認識してログインを受け付け、内部的に安全な
     形式へ自動的に再ハッシュする。user_loginなど他の列は関数を指定せず素の文字列で編集する。
  5. パスワードの値そのものの入力は、Browser Claudeではなく必ずAndo様ご自身が行うこと
     (words-for-soul.com側の運用ルールと同様)。
- Cocoonテーマの「Cocoon設定→全体→ファビコン」欄には独自のアップロード機能はなく、
  「外観→カスタマイズ→サイト基本情報→サイトアイコン」(=「設定→一般→サイトアイコン」と同じ
  項目)を使う仕様。反映されない場合はCocoonのキャッシュ(「Cocoon設定→キャッシュ削除」)を
  疑う(ただし今回の未反映の real 原因は上記の「wwwなしアクセス」だった)。
## 確立した検証方法・教訓(words-for-soul.com側と共通)
- 外部からページを取得してAIが要約する方式(WebFetchなど)は一次情報として信頼しない。
- GitHub上のファイル内容の確認は、`curl -s -H "Cache-Control: no-cache"
  "https://raw.githubusercontent.com/<owner>/<repo>/main/<file>?nocache=$(date +%s)"`のように
  キャッシュを回避したcurl直接取得を使う。
- Google検索結果(site:検索など)の確認は、Claude(Cowork)側のWebSearchツールでは`site:`演算子が
  正しく機能しないため、Browser Claudeによる実ブラウザでの確認を使う。
- 長文のテキストをBrowser Claude/ChatGPTに渡す際、コピー&ペーストの途中で文章が切れる事故が
  起きやすいので注意する。
- ファイル命名規則: Words for Soulと東京散歩人の両プロジェクトに、役割は同じだが中身が異なる
  ファイル(カスタム指示文など)を用意する場合、同じファイル名を使い回さない。ファイル名自体に
  `_words-for-soul`/`_tokyo-sanpo`のようにブログ名を含め、見ただけでどちらの内容か分かるように
  する(例: `project_custom_instructions_tokyo-sanpo.md`)。これはこの2ファイルに限らず、今後
  同様の「内容だけ異なる対」のファイルを作る際に常に適用すること。
## 進捗ログ(PROGRESS.md、このリポジトリ)の運用ルール
- 見出しは`## YYYY-MM-DD`。同じ日に複数回追記する場合は`## YYYY-MM-DD 追記N(概要)`とする。
- 新しいブロックは、ファイル冒頭の説明文のすぐ下、既存の一番上のブロックの直前に追加する
  (既存内容は削除・書き換えしない、上に積み増していく形)。
- コミットメッセージは既定のまま(「Update PROGRESS.md」)でよく、mainブランチに直接コミット
  する運用としている。
## 現在進行中・未解決の課題
- 実記事はまだ1本も執筆・公開されていない。第1回取材(東京駅→日本橋→人形町)をAndo様ご自身が
  実施しない限り、本文の執筆に着手できない。
- Google Search Console等のSEO関連設定は未着手(words-for-soul.comで確立した手順をそのまま
  流用できる見込み)。
